#!/bin/sh

# Should be a newline-separated list of homebrew HEAD taps you'd like to update.
list="$XDG_CONFIG_HOME/homebrew/head_taps"
brew_command="brew reinstall --HEAD ${p} --with-ssl"

# Simple script to update all --HEAD taps you have set up with Homebrew.
while read p; do
    echo "Running $brew_command"
    $brew_command || break
done < "$list"
